"""FoodGrade I"""
def check(namnak, grade):
    """_"""
    if namnak >= 50 and namnak <= 70:
        return  grade + 1
    else:
        return grade

def main():
    """50-70"""
    grade = 0

    grade = check(int(input()), grade)
    grade = check(int(input()), grade)
    grade = check(int(input()), grade)
    grade = check(int(input()), grade)
    grade = check(int(input()), grade)
    grade = check(int(input()), grade)
    grade = check(int(input()), grade)
    grade = check(int(input()), grade)
    grade = check(int(input()), grade)
    grade = check(int(input()), grade)
    grade = check(int(input()), grade)
    grade = check(int(input()), grade)
    grade = check(int(input()), grade)
    grade = check(int(input()), grade)
    grade = check(int(input()), grade)
    grade = check(int(input()), grade)
    grade = check(int(input()), grade)
    grade = check(int(input()), grade)
    grade = check(int(input()), grade)
    grade = check(int(input()), grade)
    grade = check(int(input()), grade)
    grade = check(int(input()), grade)
    grade = check(int(input()), grade)
    grade = check(int(input()), grade)

    print(grade)
main()
